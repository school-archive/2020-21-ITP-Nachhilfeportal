<?php
class Authentication {
    private $microsoft_token;
    private $user;

    /**
     * sets the session variables to log the user in
     * @param $token token obtained from Microsoft API
     */
    static function login($token) {

    }
}